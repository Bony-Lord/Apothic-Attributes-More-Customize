# Contributing

Use Java 21 and run `./gradlew build` before submitting a pull request. Keep the mod client-only and avoid changing real player attributes. Bug reports should include the client config, `latest.log`, the exact attribute registry IDs, and the versions of NeoForge, Apothic Attributes, and the supplying mods.
